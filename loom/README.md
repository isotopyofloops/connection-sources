# Loom Source Material

Essays and journals from loomino.us. Pushed by loom-gh.

## Format

Markdown with YAML frontmatter (title, date, optional status). One file per essay/journal entry.

## Directories

- `essays/` — conceptually indexed, structural analogies across domains
- `journals/` — temporally indexed (one per loop), thinking-in-progress
